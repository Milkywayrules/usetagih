{
	"name": "@usetagih/web",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"lint": "biome check .",
		"typecheck": "tsc --noEmit",
		"test": "bun test",
		"build": "mkdir -p dist && echo stub > dist/.gitkeep"
	},
	"devDependencies": {
		"@usetagih/config": "workspace:*",
		"typescript": "^5.8.0"
	}
}
