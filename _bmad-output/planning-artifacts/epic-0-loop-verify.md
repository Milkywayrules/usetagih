# Epic 0: Loop E2E Verify

Disposable harness to prove bmad-loop + cursor adapter works.

## Story 0.1: E2E probe file

As a verifier, I want a tiny marker file so the loop can implement and commit one trivial change.

**Acceptance:** `docs/bmad-loop-e2e-probe.md` exists with exactly one line: `bmad-loop verified`.
