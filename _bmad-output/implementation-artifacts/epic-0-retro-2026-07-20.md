# Epic 0 Retrospective — Monorepo & CI Foundation

**Project:** usetagih  
**Date:** 2026-07-20  
**Facilitator:** Amelia (Developer)  
**Project Lead:** King (headless synthesis — no human in loop)  
**Epic status:** complete (6/6 stories done, epic-0 closed)

---

## Epic summary

Epic 0 established the Bun + turborepo monorepo substrate, shared lint/typecheck config, local Docker stack, GitHub Actions CI skeleton, GHCR publish workflow skeleton, and Doppler/env validation scaffold. All six stories reached `done` with code review on 0-1 through 0-6.

| Metric | Value |
| --- | --- |
| Stories completed | 6 / 6 (100%) |
| Epic gate for Epic 1 | satisfied — monorepo/CI foundation ready |
| Turbo baseline | 36/36 tasks (`lint`, `typecheck`, `test`, `build`) |
| Production incidents | 0 (local-only repo, no deploy) |
| Environment-gated ACs | GitHub runner (0-4, 0-5), GHCR push (0-5), Doppler dashboard (0-6), Docker runtime (0-3 when daemon unavailable) |

### Delivery commits (implementation)

```
404d0f0 build: initialize turborepo monorepo workspace with bun and biome
7e31fdd build: add shared typescript and biome config package
d69b945 build: add local docker compose for postgres and minio
00075aa ci: add github actions workflow for lint typecheck test build
e2fb71c ci: add ghcr build and push workflow skeleton
7f36e66 feat: add doppler mapping and zod env validation scaffold
1ec6400 chore: mark story 0-6 done and close epic 0
```

All epic-0 implementation commits verified **clean** (no `Co-authored-by` trailer in history — agents used `commit-tree` escape hatch).

---

## Team participants

- Amelia (Developer) — facilitation
- Alice (Product Owner)
- Charlie (Senior Dev)
- Dana (QA Engineer)
- Elena (Junior Dev)
- King (Project Lead) — synthesized headless

---

## Part 1 — Epic review

### What went well

**Charlie (Senior Dev):** Story 0-2's `@usetagih/config` package with `${configDir}` presets became the pattern every later story reused — one shared tsconfig/biome source of truth.

**Dana (QA Engineer):** Code reviewers consistently re-ran verification with `--force` when dev agents reported `FULL TURBO, 0ms` cache hits (0-2, 0-4). That caught a real blind spot.

**Alice (Product Owner):** Environment-gated acceptance criteria let the epic finish locally without a GitHub remote or Doppler workspace, while keeping a clear re-verify contract for first push.

**Amelia (Developer):** Story specs with zero-guess file paths, grep verification commands, and out-of-scope fences kept agents from scope-creeping into Epic 1 (Typst, real Spectral, production Dockerfiles).

### Challenges (systems, not blame)

| Theme | Evidence | Stories |
| --- | --- | --- |
| Cursor `Co-authored-by` trailer on every `git commit` | Orchestrator observed; all subagents forced through `commit-tree` escape hatch | all |
| Turbo cache masked verification | Dev reported `FULL TURBO, 0ms`; reviewer cleared with `--force` | 0-2, 0-4, 0-5 |
| Runtime ACs blocked by environment | No GitHub remote, no Doppler token, Docker daemon optional | 0-3, 0-4, 0-5, 0-6 |
| Biome scope on symlinked paths | Root biome check failed on `.agents/` and knowledge-base symlink | 0-1 |
| MinIO mc image tag drift | Pinned tag not on Docker Hub; dev picked nearest prior release | 0-3 |
| Zod 4.x biome nits | Barrel export + `type` vs `interface` lint on env scaffold | 0-6 |

### Recurring review feedback

- Re-run turbo with `--force` before approving when cache timing looks suspicious (0-4 Code Review Record).
- Defer environment-only mismatches (local Bun 1.3.x vs pinned 1.2.x band) when lockfile and CI pin align (0-1).
- Keep GHCR/docker disk cleanup mandatory after local stub builds (0-5).

### Breakthrough moments

- **0-2:** Shared config package eliminated per-package tsconfig drift and removed Story 0-1's test-file exclude stopgap.
- **0-3→0-6:** Environment-gated pattern propagated cleanly — static YAML/grep verification locally, runtime ACs documented for operator re-verify.

### Previous retro follow-through

First formal epic retrospective (Epic 0 harness retro remains `optional`). No prior action items to track.

---

## Part 2 — Next epic preparation (Epic 1: PDF Pipeline Spike)

**Epic 1 title:** PDF Pipeline Spike (BLOCKING GATE)  
**Dependencies on Epic 0:** monorepo builds, CI workflow, `docker/Dockerfile.render-ci` stub (real image in Story 1.4), GHCR publish skeleton.

### Critical preparation before Epic 1 kickoff

| Item | Owner | Notes |
| --- | --- | --- |
| Re-verify GitHub Actions CI on first remote push | Charlie (Senior Dev) | 0-4 runner AC was environment-gated |
| Re-verify GHCR publish workflow on first push to `main` | Charlie (Senior Dev) | 0-5 push AC environment-gated |
| Replace render-ci alpine stub with debian+Typst image | Charlie (Senior Dev) | Story 1.4 scope |
| Doppler project `usetagih` + configs dev/staging/prod | King (operator) | 0-6 dashboard setup environment-gated |

### Significant discovery assessment

**Epic update required:** NO — Epic 1 plan still aligns. Epic 0 surfaced process gaps (hooks, turbo `--force`, environment-gated docs) but not architectural surprises.

---

## Readiness assessment

| Dimension | Status |
| --- | --- |
| Testing & quality | Local turbo 36/36 green; CI YAML structurally valid; runner execution pending remote |
| Deployment | Not applicable — no production deploy in Epic 0 |
| Stakeholder acceptance | Foundation epic — King ratified via story completion |
| Technical health | Stable scaffold; known deferred items fenced in stories |
| Unresolved blockers | First-push re-verification for GitHub/GHCR/Doppler |

**Verdict:** Epic 0 is complete from a story perspective. Epic 1 may start after critical-path re-verification items are tracked (not blocking story file creation).

---

## Action items

| # | Action | Owner | Status | Success criteria |
| --- | --- | --- | --- | --- |
| 1 | Wire `commit-msg` hook via `scripts/setup-verasic-git-hooks.sh` | Charlie (Senior Dev) | **done** | Hook strips injected trailers; verify prints `clean` on normal commits |
| 2 | Standardize `--force` on turbo verification commands in story templates | Amelia (Developer) | open | Story verification sections include `bunx turbo run lint typecheck test build --force` |
| 3 | Document environment-gated AC re-verification checklist for first push | Dana (QA Engineer) | open | Single doc lists GitHub CI, GHCR push, Doppler setup re-verify steps |
| 4 | Run first-push environment re-verification when remote exists | King (Project Lead) | open | CI green on GitHub; GHCR tags pushed; Doppler project live |

---

## Key takeaways

1. **Deterministic git hooks beat escape hatches** — wiring `core.hooksPath` removes trailer tax on every agent commit.
2. **Turbo cache reports are not proof** — reviewers must default to `--force` for story sign-off.
3. **Environment-gated ACs work** — explicit re-verify contracts let local-only development finish without faking runtime checks.

---

## Commitments

- Action items recorded in `sprint-status.yaml`
- `epic-0-retrospective` → `done`
- Hook wiring executed and verified during this retro (commit `915545a`)

---

*Headless retrospective — synthesized from story Dev Agent Records, Code Review Records, git history, and orchestrator friction signals.*
