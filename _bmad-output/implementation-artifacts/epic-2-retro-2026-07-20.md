# Epic 2 Retrospective — Canonical Schema & Contract Package

**Project:** usetagih  
**Date:** 2026-07-20  
**Facilitator:** Amelia (Developer)  
**Project Lead:** King (headless synthesis — no human in loop)  
**Epic status:** complete (6/6 stories done, epic-2 closed)

---

## Epic summary

Epic 2 delivered `@usetagih/schema` as the single source of truth for document payloads, arithmetic validation, error envelopes, schema-version negotiation, OpenAPI 3.1 component generation, and a sidecar-driven fixture corpus (SM-2). All six stories reached `done` with adversarial code review on every story.

| Metric | Value |
| --- | --- |
| Stories completed | 6 / 6 (100%) |
| Epic gate for Epic 3 | satisfied — canonical contract package ready |
| Turbo baseline | 36/36 tasks (`lint`, `typecheck`, `test`, `build`) |
| Schema unit tests | 56 pass (`bun test packages/schema` at Epic 2 close) |
| Production incidents | 0 (pre-API epic) |
| Adversarial reviews | 6 / 6 stories reviewed; real defects caught in 2.1, 2.3, 2.5, 2.6 |

### Delivery commits (implementation highlights)

```
808fd81 feat: add document payload zod discriminated union
93e2059 feat: add arithmetic integrity validators
06a82a9 feat: add error codes enum and api error envelope
42769ff feat: add schema version negotiation helpers
0abc3cb feat: add openapi component generation from zod
81f583a fix(schema): harden openapi generation review gaps
7c452a5 test: add shared validation fixture suite
9ed058b chore: mark story 2-6 done and close epic 2
```

Epic 2 commits verified **clean** via wired `commit-msg` hook (Epic 0 retro action item — no `commit-tree` escape hatch needed this epic).

---

## Team participants

- Amelia (Developer) — facilitation
- Alice (Product Owner)
- Charlie (Senior Dev)
- Dana (QA Engineer)
- Elena (Junior Dev)
- King (Project Lead) — synthesized headless

---

## Part 1 — Epic review

### What went well

**Charlie (Senior Dev):** Story 2.2's bigint scaled-integer decimal math survived 9,621 scale-combo probes and a 999,999-entry quantity brute-force in review — the arithmetic layer is trustworthy for Epic 3 validate/render paths.

**Dana (QA Engineer):** Story 2.6's sidecar manifest (25 entries, 100% path+code on failures) gives Epic 3 a concrete validation trust anchor instead of ad-hoc inline tables.

**Alice (Product Owner):** AD-1 held — duplicate-Zod guard scans `packages/*/src` and `apps/*/src` with a single env-schema allowlist. One Zod owner survived the epic.

**Amelia (Developer):** Adversarial code review caught real defects on **every story that needed fixes** (2.1, 2.3, 2.5, 2.6). The review stage is paying for itself.

**King (Project Lead):** Epic 0's wired `commit-msg` hook eliminated trailer escape-hatches for the entire epic — deterministic git hygiene without agent workarounds.

### Challenges (systems, not blame)

| Theme | Evidence | Stories |
| --- | --- | --- |
| Scope creep in schema-only stories | Dev touched `packages/core`, `packages/sdk`, `packages/render` (preview wiring) in a structural schema story | 2.1 |
| Duplicate test execution | `*.test.ts` compiled into `dist/`; bun ran tests twice (src + dist) until build exclude added | 2.1 |
| Env-resource flakiness under parallel turbo | Typst preview tests hit 5s default timeout (~15s under load); not a temp-dir race | 2.3 |
| Silent turbo cache gap for generated artifacts | `openapi/**` missing from `build.outputs`; cache hit skipped `components.json` regeneration | 2.5 |
| Over-broad biome exclusion | `!!**/openapi` excluded `src/openapi/` source from lint | 2.5 |
| Contract change smuggled into test story | `isoCurrencySchema` XXX rejection added in 2.6; runtime used `refine()` but OpenAPI still emitted `^[A-Z]{3}$` | 2.6 |

### Recurring review feedback

- Re-run turbo with `--force` before sign-off (continued from Epic 0).
- Generated-artifact stories must declare `turbo.json` outputs **and** narrow biome excludes (generated dir only).
- Schema/contract changes require OpenAPI structural test parity in the same change set.
- Typst/render integration tests need explicit timeout budgets when run under parallel turbo.

### Breakthrough moments

- **2.2:** Bigint decimal math pattern — reusable for all money arithmetic; no float on money strings.
- **2.3:** Closed 13-code enum + HTTP 1:1 map — Epic 3 middleware can import stable symbols.
- **2.6:** Table-driven fixture runner with sidecars — SM-2 metric automatable; Epic 7 SDK parity inherits same corpus.

### Previous retro follow-through (Epic 0)

| Action item | Status | Epic 2 evidence |
| --- | --- | --- |
| Wire `commit-msg` hook | ✅ **done** | All Epic 2 commits clean without escape hatch |
| Standardize `--force` in story templates | ⏳ **open → done this retro** | Stories already used `--force` in ACs; template now codifies it |
| Document environment-gated AC re-verify checklist | ❌ **not addressed** | Still open — no remote push yet |
| Run first-push environment re-verification | ❌ **not addressed** | Blocked on remote existence |

---

## Part 2 — Next epic preparation (Epic 3: API Core)

**Epic 3 title:** API Core — Auth, Keys, Idempotency, Sync Render, Share, Audit  
**Dependencies on Epic 2:** `validateDocumentPayload()`, error envelope builders, schema metadata, OpenAPI components, fixture corpus, AD-1 single Zod owner.

### Known upcoming scope (correct-course, NOT processed in this retro)

King added three directives in `HARNESS-ADDITIONAL-INSTRUCTIONS.md` that require a **correct-course amendment pass before Epic 3 kickoff**:

1. **UI stack:** base-ui, not radix-ui (refactor + QA if radix present).
2. **Pricing:** 4-tier model with one free/trial base tier.
3. **Multi-tenancy:** better-auth workspaces — each user must belong to ≥1 workspace; multi-tenant, not multi-team.

These are **known scope amendments** — Epic 3 stories as written may need updating after correct-course runs. Do not start Epic 3 implementation until the amendment pass lands.

### Critical preparation before Epic 3 kickoff

| Item | Owner (workflow-stage) | Notes |
| --- | --- | --- |
| Run correct-course amendment for HARNESS directives | correct-course | base-ui, 4-tier pricing, multi-tenant workspaces |
| Epic 3 story refresh after amendment | sprint-planning | Reconcile epics.md / story files with amended PRD |
| Reuse 2.6 fixture corpus in validate use-case (Story 3.2) | dev-story | Sidecar manifest is the trust anchor |
| Typst preview timeout policy under CI parallel load | code-review | 30s band-aid in 2.3; monitor for systemic CI resource limits |

### Significant discovery assessment

**Epic update required:** YES — but via **correct-course** (HARNESS directives), not Epic 2 technical findings. Epic 2 deliverables are stable; Epic 3 planning assumptions (auth model, pricing, UI primitives) need amendment before kickoff.

---

## Readiness assessment

| Dimension | Status |
| --- | --- |
| Testing & quality | 56 schema tests pass; 36/36 turbo green; SM-2 at 100% path+code on failure fixtures |
| Deployment | Not applicable — schema package, no API deploy |
| Stakeholder acceptance | Contract epic — King ratified via story completion |
| Technical health | Stable `@usetagih/schema`; known typst timeout sensitivity under parallel load |
| Unresolved blockers | Correct-course amendment for HARNESS directives before Epic 3 |

**Verdict:** Epic 2 is complete from a story perspective. Epic 3 must wait for correct-course amendment pass, then story refresh.

---

## Action items

| # | Action | Owner (workflow-stage) | Status | Success criteria |
| --- | --- | --- | --- | --- |
| 1 | Add scope-boundary gate: dev agent lists touched packages vs story out-of-scope fence before implementation | dev-story | open | Story checklist blocks files outside declared package unless explicit AC |
| 2 | Require `tsconfig.build.json` exclude `*.test.ts` when package build emits to `dist/` | dev-story | open | No duplicate test runs from dist artifacts |
| 3 | Generated-artifact checklist: declare `turbo.json` `outputs` + narrow biome exclude for generated dirs only | code-review | open | Review rejects missing outputs or broad `!!**/artifact` excludes |
| 4 | Contract/schema changes must update OpenAPI structural tests + runtime Zod in same change set | code-review | open | No runtime/OpenAPI drift (pattern: 2.6 XXX currency) |
| 5 | Typst/render tests under parallel turbo: explicit timeout ≥30s or serial isolation flag | code-review | open | Preview tests stable under `turbo run test --force` |
| 6 | Standardize `bunx turbo run lint typecheck test build --force` in story template verification section | create-story | **done** | Template includes `--force` command |
| 7 | Run correct-course amendment for HARNESS directives (base-ui, 4-tier pricing, multi-tenant workspaces) | correct-course | open | Amended PRD/epics before Epic 3 story creation |
| 8 | Document environment-gated AC re-verification checklist (carry-forward from Epic 0) | sprint-planning | open | Single doc for first-push re-verify steps |

---

## Key takeaways

1. **Adversarial review is the quality gate** — scope creep, cache gaps, OpenAPI drift, and duplicate tests were all caught in review, not in dev self-check.
2. **Generated artifacts need turbo outputs + narrow biome excludes** — silent cache misses and unlinted source dirs are a repeatable failure class.
3. **Contract changes belong in contract stories** — test-only stories must not smuggle schema changes without OpenAPI parity.
4. **Epic 0 hook wiring compounded** — zero trailer tax across Epic 2; deterministic hooks beat per-commit escape hatches.

---

## Commitments

- Action items recorded in `sprint-status.yaml`
- `epic-2-retrospective` → `done`
- Story template + dev checklist updated with mechanical gates from this retro

---

*Headless retrospective — synthesized from story Dev Agent Records, Senior Developer Review sections, git history, and orchestrator friction signals.*
